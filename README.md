# **<p align="center">neural-network-challenge-2 </p>**

In this challenge, the attrition data is in csv file is analyzed.

- The data is first prepared by loading it into a data frame and two targets department and attrition are identified. 
- Then testing and training datasets are created and standard scalar and onehotencoder are applied
- The the neural network model is created , trained for two output layers and it is evaluated
